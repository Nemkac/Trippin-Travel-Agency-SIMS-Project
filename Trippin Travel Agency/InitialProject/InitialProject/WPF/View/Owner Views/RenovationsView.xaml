<UserControl x:Class="InitialProject.WPF.View.Owner_Views.RenovationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InitialProject.WPF.View.Owner_Views"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="710">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical"
                            Margin="0,0,0,0" VerticalAlignment="Center">
            <Label Content="Here is a list of your renovations" Style="{StaticResource defaultLabel}"
               Grid.Row="0" FontSize="20" FontWeight="Light" VerticalContentAlignment="Center"/>
            <!-- Choose a tour and click a button beneath -->
            <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Left" Margin="20 20 0 0">
                <fa:IconImage Icon="Fingerprint"
                                      HorizontalAlignment="Left"
                                      Style="{StaticResource menuBackButtonIcon}"
                                      Width="15" Height="15" Foreground="#353b48"/>
                <Label Content="Choose an accommodation and click a button beneath to cancel the renovation"
                               Style="{StaticResource labelAboveTextBox}"
                               Foreground="#353b48"
                               Margin="10,0,0,0"
                               />
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <DataGrid Style="{DynamicResource DataGridStyle1}" 
                  CellStyle="{DynamicResource DataGridCellStyle1}" 
                  ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" 
                  RowStyle="{DynamicResource DataGridRowStyle1}" x:Name="monthlyStatisticsDataGrid" 
                  AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Accommodation name"  Width="Auto"/>
                    <DataGridTextColumn Header="Accommodation type" Width="Auto"/>
                    <DataGridTextColumn Header="Start date" Width="Auto"/>
                    <DataGridTextColumn Header="End date" Width="Auto"/>
                    <DataGridTextColumn Header="Duration" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>

        <Button Style="{StaticResource showImagesButton}"
                Grid.Row="3" HorizontalAlignment="Right"
                BorderBrush="Transparent" Width="280" Height="35" Margin="0 0 10 0">
            <StackPanel Orientation="Horizontal"
                                Margin="0, 0, 0 ,0">
                <fa:IconImage Icon="Gear" HorizontalAlignment="Left" Width="20" Height="20" Foreground="#192a56" VerticalAlignment="Center" Margin="0 2 0 0"/>
                <Label VerticalAlignment="Center" Foreground="#192a56" Content="Schedule new renovation" FontSize="20" FontFamily="Ubuntu" FontWeight="Regular"  Margin="10 0 0 0"/>
            </StackPanel>
        </Button>
        <Button Style="{StaticResource denyButtonTablet}"
                Grid.Row="3" HorizontalAlignment="Left"
                BorderBrush="Transparent" Width="280" Height="35" Margin="10 0 0 0">
            <StackPanel Orientation="Horizontal"
                                Margin="0, 0, 0 ,0">
                <fa:IconImage Icon="Xmark" HorizontalAlignment="Left" Width="20" Height="20" Foreground="#F4FFF8" VerticalAlignment="Center" Margin="0 2 0 0"/>
                <Label VerticalAlignment="Center" Foreground="#F4FFF8" Content="Cancel renovation" FontSize="20" FontFamily="Ubuntu" FontWeight="Regular" Margin="10 0 0 0"/>
            </StackPanel>
        </Button>

    </Grid>
</UserControl>
