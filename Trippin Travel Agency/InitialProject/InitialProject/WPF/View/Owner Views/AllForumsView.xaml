<UserControl x:Class="InitialProject.WPF.View.Owner_Views.AllForumsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InitialProject.WPF.View.Owner_Views"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="710">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1">
            <ListBox Height="550"
             Background="Transparent"
             BorderBrush="Transparent"
             Foreground="#192a56"
             ItemsSource="{Binding Forums}"
             FontFamily="Ubuntu"
             FontSize="23"
             FontWeight="Regular">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0 10 0 0">
                            <TextBlock Text="{Binding}" FontSize="26" FontWeight="Bold"/>
                            <TextBlock Text="{Binding}" FontSize="18" FontWeight="Regular"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</UserControl>
